<%= render 'shared/header', title: 'Create a New Status' %>

<%= render 'shared/error', obj: @status %>

<%= form_for @status do |f| %>
  <%= f.label :body %>
  <br/>
  <%= f.text_area :body, rows: 4 %>
  </br>
  <%= f.submit 'Create Status' %>
<% end %>